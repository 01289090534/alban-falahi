(()=>{window.addEmployee=async()=>{const branches=await api('branches?select=id,name_ar&is_active=eq.true&order=name_ar.asc');const d=document.createElement('div');d.className='dash-modal-back';d.innerHTML='<div class="dash-modal"><div class="head"><h2>إضافة مستخدم لبرنامج المحل</h2><button class="btn light" id="suClose">إغلاق</button></div><p class="sub">الحساب سيكون له كل صلاحيات برنامج المحل، ومربوط بالفرع المختار.</p><div class="settings-grid"><div class="field"><label>الاسم</label><input id="suName"></div><div class="field"><label>رقم الموبايل</label><input id="suPhone" inputmode="tel"></div><div class="field"><label>كلمة المرور</label><input id="suPass" type="password" minlength="6"></div><div class="field"><label>الفرع</label><select id="suBranch">'+branches.map(b=>'<option value="'+b.id+'">'+esc(b.name_ar)+'</option>').join('')+'</select></div></div><div id="suMsg"></div><button class="btn" id="suSave">إنشاء الحساب</button></div></div>';document.body.appendChild(d);d.querySelector('#suClose').onclick=()=>d.remove();d.querySelector('#suSave').onclick=async()=>{const btn=d.querySelector('#suSave');btn.disabled=true;try{const name=d.querySelector('#suName').value.trim(),phone=d.querySelector('#suPhone').value.trim(),password=d.querySelector('#suPass').value,branch_id=d.querySelector('#suBranch').value;if(!name||!phone||!password||!branch_id)throw Error('املأ كل البيانات');const r=await fetch(SUPA+'/functions/v1/admin-auth',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+session.access_token},body:JSON.stringify({action:'create_shop_user',full_name:name,phone,password,branch_id})});const x=await r.json();if(!r.ok||!x.success)throw Error(x.error||'تعذر إنشاء الحساب');alert('تم إنشاء حساب برنامج المحل بنجاح ✅');d.remove();employees()}catch(e){d.querySelector('#suMsg').innerHTML='<div class="notice bad">'+esc(e.message)+'</div>'}finally{btn.disabled=false}}};const oldEmployees=window.employees;window.employees=async()=>{await oldEmployees();const head=document.querySelector('#content .head');if(head&&!document.getElementById('shopUserAdd')){const b=document.createElement('button');b.id='shopUserAdd';b.className='btn';b.textContent='🥛 إضافة مستخدم برنامج المحل';b.onclick=window.addEmployee;head.appendChild(b)}}})();