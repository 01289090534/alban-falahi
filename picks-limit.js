(()=>{const limit=()=>{const box=document.getElementById('products');if(!box)return;const title=document.querySelectorAll('section .title')[1]?.textContent||'';if(!/اختيارات/.test(title))return;const cards=[...box.querySelectorAll('.product')];cards.forEach((el,i)=>{el.style.display=i<6?'flex':'none'});};window.addEventListener('load',()=>{limit();setInterval(limit,500)});new MutationObserver(limit).observe(document.documentElement,{subtree:true,childList:true});})();
