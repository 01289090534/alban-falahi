const BASE = (import.meta.env.VITE_HR_API_URL || 'https://roycuuyceopmlmkjllrt.supabase.co/functions/v1/hr-api').replace(/\/$/,'');
async function request(path:string, init:RequestInit={}){const res=await fetch(`${BASE}${path}`,{...init,credentials:'include',headers:{'Content-Type':'application/json',...(init.headers||{})}});const text=await res.text();let data:any={};try{data=text?JSON.parse(text):{}}catch{data={message:text}};if(!res.ok)throw new Error(data?.error||data?.message||`HTTP ${res.status}`);return {data};}
export const api={get:(path:string)=>request(path),post:(path:string,body:any)=>request(path,{method:'POST',body:JSON.stringify(body)}),put:(path:string,body:any)=>request(path,{method:'PUT',body:JSON.stringify(body)}),delete:(path:string)=>request(path,{method:'DELETE'})};
export const auth={signOut:async()=>{try{await request('/api/logout',{method:'POST',body:'{}'})}catch{}},signIn:async()=>{throw new Error('Owner login now uses the HR username/password login.')}};
