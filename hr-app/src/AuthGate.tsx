import { useEffect, useState } from 'react';
import { api, auth } from '@appdeploy/client';
export default function AuthGate({ children }: { children: React.ReactNode }) { const [signedIn,setSignedIn]=useState(auth.isSignedIn()); const [loading,setLoading]=useState(true); const [error,setError]=useState(''); useEffect(()=>{const boot=async()=>{try{const user=await auth.getUser();if(user){await api.get('/api/me');setSignedIn(true)}}catch(e){setError('تعذر التحقق من الحساب. حاول تسجيل الدخول مرة أخرى.')}finally{setLoading(false)}};void boot()},[]);const login=async()=>{setError('');try{await auth.signIn({scope:'openid email profile offline_access'});await api.get('/api/me');setSignedIn(true)}catch(e:any){const code=e?.code;setError(code==='popup_blocked'?'اسمح بالنوافذ المنبثقة لتسجيل الدخول.':code==='popup_closed'?'تم إلغاء تسجيل الدخول.':'تعذر تسجيل الدخول. حاول مرة أخرى.')}};if(loading)return <div className='authScreen'><div className='authCard'><div className='brandMark'>أ</div><h1>ألبان فلاحي</h1><p>جاري التحقق من حساب الداش بورد...</p></div></div>;if(!signedIn)return <div className='authScreen'><div className='authCard'><div className='brandMark'>أ</div><h1>ألبان فلاحي</h1><h2>تسجيل دخول الداش بورد</h2><p>الدخول محمي بحسابك الموثّق.</p><button className='primary authButton' onClick={login}>تسجيل الدخول</button>{error&&<div className='authError'>{error}</div>}</div></div>;return <>{children}</> }
