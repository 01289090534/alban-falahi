self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?.text()||'وصل طلب توصيل جديد'}};event.waitUntil(self.registration.showNotification(data.title||'🚨 طلب توصيل جديد - ألبان فلاحي',{body:data.body||'تم تعيين طلب توصيل لك',tag:data.tag||'alban-driver-order',renotify:true,requireInteraction:true,vibrate:[500,120,500,120,800],data:{url:data.url||'/driver-live?v=33'}}))});self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/driver-live?v=33';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus' in c){c.focus();return c.navigate(url)}}return clients.openWindow(url)}))});