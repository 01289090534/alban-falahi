const SUPABASE_URL='https://qxwvuxkbcghbkztjrzon.supabase.co';
const nav=[['dashboard','📊','الرئيسية'],['branches','🏪','الفروع'],['categories','📂','الأقسام'],['products','🧀','المنتجات'],['availability','🟢','التوافر'],['orders','🛵','الطلبات'],['customers','👥','العملاء'],['drivers','🚗','السائقين'],['offers','🎁','العروض'],['reports','📈','التقارير']];
let current='dashboard';
const app=document.getElementById('app');
function renderShell(){app.innerHTML=`<div class="layout"><aside class="sidebar"><div class="brand">🥛 ألبان فلاحي</div><div class="nav">${nav.map(x=>`<button class="${x[0]===current?'active':''}" data-page="${x[0]}">${x[1]} &nbsp; ${x[2]}</button>`).join('')}</div></aside><main class="main"><div class="top"><div><div class="title">${nav.find(x=>x[0]===current)?.[2]||'الرئيسية'}</div><div class="sub">إدارة نظام ألبان فلاحي للتوصيل</div></div><div class="user">👤 مدير النظام</div></div><div id="content"></div></main></div>`;document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{current=b.dataset.page;renderShell();renderPage()})}
async function get(path){try{const r=await fetch(`${SUPABASE_URL}/rest/v1/${path}`,{headers:{apikey:window.SUPABASE_ANON_KEY||'',Authorization:`Bearer ${window.SUPABASE_ANON_KEY||''}`}});return r.ok?await r.json():[]}catch{return[]}}
async function renderPage(){const c=document.getElementById('content');if(current==='dashboard'){const [branches,cats,products,orders]=await Promise.all([get('branches?select=id,is_active'),get('categories?select=id,is_active'),get('products?select=id,is_active'),get('orders?select=id,status')]);c.innerHTML=`<div class="cards"><div class="card"><div class="label">الفروع</div><div class="value">${branches.length}</div></div><div class="card"><div class="label">الأقسام</div><div class="value">${cats.length}</div></div><div class="card"><div class="label">المنتجات</div><div class="value">${products.length}</div></div><div class="card"><div class="label">الطلبات</div><div class="value">${orders.length}</div></div></div><div class="section"><h2>الفروع</h2><div class="branch-grid">${branches.length?branches.map(b=>`<div class="card branch"><span>🏪 فرع</span><span class="badge">${b.is_active?'نشط':'غير نشط'}</span></div>`).join(''):'<div class="card empty">لا توجد بيانات بعد</div>'}</div></div>`}else{const labels={branches:'إدارة الفروع وإضافة فرع جديد',categories:'إدارة الأقسام والصور والترتيب',products:'إضافة وتعديل المنتجات والأسعار',availability:'تفعيل وتعطيل المنتجات لكل فرع',orders:'متابعة الطلبات وحالاتها',customers:'بيانات العملاء وطلبات إعادة الشراء',drivers:'إدارة السائقين وتوزيع الطلبات',offers:'العروض والكوبونات',reports:'تقارير المبيعات والأداء'};c.innerHTML=`<div class="card"><h2>${labels[current]}</h2><div class="notice">الشاشة الأساسية جاهزة، والخطوة التالية هي ربط عمليات الإضافة والتعديل والحذف مباشرة بقاعدة البيانات مع الصلاحيات.</div></div>`}}
renderShell();renderPage();