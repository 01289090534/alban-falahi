import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';export default function Payroll(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase?.from('hr_v2_payroll').select('*').order('period_end',{ascending:false}).limit(100).then(({data})=>setRows(data||[]))},[]);return <><div className="page-head"><h2>الرواتب</h2></div><div className="panel table-wrap"><table><thead><tr><th>الموظف</th><th>الفترة</th><th>الأساسي</th><th>الإضافي</th><th>الخصومات</th><th>السلف</th><th>الصافي</th><th>الحالة</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.employee_id}</td><td>{r.period_start} → {r.period_end}</td><td>{r.base_salary}</td><td>{r.overtime_total}</td><td>{r.deductions}</td><td>{r.advances}</td><td>{r.net_salary}</td><td>{r.status}</td></tr>)}</tbody></table></div></>}