import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';import logoData from '../assets/logoData';
const url=(import.meta.env.VITE_SUPABASE_URL as string|undefined)||'https://roycuuyceopmlmkjllrt.supabase.co';const key=(import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY as string|undefined)||'sb_publishable_fG1i8HSgImBQMdSgj_4q6Q_CBE9Gw21';const fn=`${url}/functions/v1/hr-v2-bootstrap`;
const wait=<T,>(promise:PromiseLike<T>,ms=15000)=>Promise.race([promise,new Promise<never>((_,reject)=>setTimeout(()=>reject(new Error('انتهت مهلة الاتصال. حاول مرة أخرى.')),ms))]);
async function bootstrapStatus(){const r=await wait(fetch(fn,{headers:{apikey:key,Authorization:`Bearer ${key}`,Accept:'application/json'}}));const data=await r.json().catch(()=>null);if(!r.ok)throw new Error(data?.error||`HTTP ${r.status}`);return data as {needs_setup?:boolean}}
async function bootstrapCreate(body:{name:string;phone:string;password:string}){const r=await wait(fetch(fn,{method:'POST',headers:{apikey:key,Authorization:`Bearer ${key}`,Accept:'application/json','Content-Type':'application/json'},body:JSON.stringify(body)}));const data=await r.json().catch(()=>null);if(!r.ok)throw new Error(data?.error||`HTTP ${r.status}`);if(data?.error)throw new Error(data.error);return data}
export default function Login(){const[id,setId]=useState(''),[pw,setPw]=useState(''),[name,setName]=useState(''),[err,setErr]=useState(''),[busy,setBusy]=useState(false),[first,setFirst]=useState(false),[checking,setChecking]=useState(true);useEffect(()=>{let alive=true;bootstrapStatus().then(v=>{if(alive)setFirst(!!v.needs_setup)}).catch(()=>{if(alive)setErr('تعذر التحقق من إعداد النظام. حاول تحديث الصفحة.')}).finally(()=>{if(alive)setChecking(false)});return()=>{alive=false}},[]);async function submit(e:any){e.preventDefault();setBusy(true);setErr('');try{const digits=id.trim().replace(/\D/g,'');if(first){if(!name.trim()||digits.length<7||pw.length<6)throw new Error('أدخل الاسم ورقم الموبايل وكلمة مرور 6 أحرف على الأقل');await bootstrapCreate({name:name.trim(),phone:digits,password:pw})}else{if(digits.length<7||pw.length<6)throw new Error('أدخل رقم الموبايل وكلمة المرور بشكل صحيح')}const r=await wait(supabase.auth.signInWithPassword({email:`${digits}@hr.alban-falahi.local`,password:pw}));if(r.error)throw r.error;window.location.replace('/');}catch(e:any){setErr(e?.message||'بيانات الدخول غير صحيحة')}finally{setBusy(false)}}return <div className="login-page"><div className="login-card"><div className="login-brand"><img src={logoData} alt="ألبان فلاحي"/><div><h1>ألبان فلاحي</h1><p>نظام إدارة الموارد البشرية</p></div></div>{checking?<div className="hint"><b>جاري التحقق من إعداد النظام...</b><br/>لحظة واحدة.</div>:first?<div className="hint"><b>الإعداد الأول للنظام</b><br/>أنشئ حساب مدير النظام الأول مرة واحدة. بعد ذلك يختفي التسجيل ويصبح إنشاء المستخدمين من داخل لوحة المدير فقط.</div>:<div className="hint"><b>تسجيل الدخول</b><br/>ادخل برقم الموبايل وكلمة المرور للوصول إلى نظام الموارد البشرية.</div>}{!checking&&<form onSubmit={submit}>{first&&<label>اسم مدير النظام<input required value={name} onChange={e=>setName(e.target.value)} /></label>}<label>رقم الهاتف أو حساب الموظف<input required value={id} onChange={e=>setId(e.target.value)} /></label><label>كلمة المرور<input required type="password" minLength={6} value={pw} onChange={e=>setPw(e.target.value)} /></label>{err&&<div className="error">{err}</div>}<button className="primary full" disabled={busy}>{busy?'جاري المعالجة...':first?'إنشاء حساب المدير الأول':'دخول'}</button></form>}<div className="login-footer">ألبان فلاحي • الموارد البشرية</div></div></div>}