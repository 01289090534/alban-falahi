import { useEffect,useState } from 'react';
import {Edit3,Plus,Search} from 'lucide-react';
import {supabase} from '../lib/supabase';
const blank={employee_no:'',full_name_ar:'',full_name_en:'',phone:'',position:'',branch_id:'',base_salary:'',pay_type:'monthly',overtime_rate:'',daily_work_hours:'8',max_daily_paid_hours:'8',is_delivery:false,payroll_cycle:'monthly',start:'09:00',end:'17:00',hire_date:'',status:'inactive',login_phone:'',login_password:''};
const blankBranch={name:'',address:'',phone:'',is_active:true};
export default function Employees({branchesOnly=false}:{branchesOnly?:boolean}){
 const[rows,setRows]=useState<any[]>([]),[branches,setBranches]=useState<any[]>([]),[q,setQ]=useState(''),[show,setShow]=useState(false),[showBranch,setShowBranch]=useState(false),[editing,setEditing]=useState<any>(null),[editingBranch,setEditingBranch]=useState<any>(null),[form,setForm]=useState<any>(blank),[branchForm,setBranchForm]=useState<any>(blankBranch),[saving,setSaving]=useState(false),[error,setError]=useState('');
 async function load(){if(!supabase)return;if(branchesOnly){const{data}=await supabase.from('hr_v2_branches').select('*').order('created_at',{ascending:false});setRows(data||[]);return}const{data}=await supabase.from('hr_v2_employees').select('*').order('created_at',{ascending:false});setRows(data||[]);const{data:b}=await supabase.from('hr_v2_branches').select('id,name').eq('is_active',true).order('name');setBranches(b||[])}
 useEffect(()=>{load()},[branchesOnly]);
 function openAdd(){setEditing(null);setForm({...blank});setError('');setShow(true)}
 async function openEdit(r:any){let account:any=null;if(supabase){const{data}=await supabase.from('hr_v2_accounts').select('id,phone,is_active').eq('employee_id',r.id).maybeSingle();account=data}setEditing({...r,account_id:account?.id,account_active:account?.is_active});setForm({...blank,...r,max_daily_paid_hours:r.max_daily_paid_hours??r.daily_work_hours??8,is_delivery:!!r.is_delivery,payroll_cycle:r.payroll_cycle||'monthly',start:r.work_schedule?.start||'09:00',end:r.work_schedule?.end||'17:00',login_phone:account?.phone||r.phone||'',login_password:''});setError('');setShow(true)}
 async function saveEmployee(){if(!supabase)return;if(!form.full_name_ar.trim())return setError('الاسم بالعربي مطلوب');if(!editing&&!form.login_phone)return setError('موبايل الدخول مطلوب');if(!editing&&!form.login_password)return setError('كلمة المرور مطلوبة');setSaving(true);setError('');const delivery=!!form.is_delivery;const payload={employee_no:form.employee_no||null,full_name_ar:form.full_name_ar.trim(),full_name_en:form.full_name_en||null,phone:form.phone||null,position:form.position||null,branch_id:form.branch_id||null,base_salary:Number(form.base_salary)||0,pay_type:delivery?'hourly':form.pay_type||'monthly',overtime_rate:Number(form.overtime_rate)||0,daily_work_hours:Number(form.daily_work_hours)||8,max_daily_paid_hours:Number(form.max_daily_paid_hours)||Number(form.daily_work_hours)||8,is_delivery:delivery,payroll_cycle:delivery?'weekly_delivery':'monthly',work_schedule:{start:form.start,end:form.end},hire_date:form.hire_date||null,status:form.status||'inactive'};let employeeId=editing?.id;let dbError:any=null;if(editing){dbError=(await supabase.from('hr_v2_employees').update(payload).eq('id',editing.id)).error}else{const r=await supabase.from('hr_v2_employees').insert(payload).select('id').single();employeeId=r.data?.id;dbError=r.error}if(!dbError&&form.login_phone){const r=await supabase.functions.invoke('hr-v2-admin-user',{body:{user_id:editing?.account_id||undefined,employee_id:employeeId,full_name:form.full_name_ar,phone:form.login_phone,password:form.login_password||undefined,is_active:form.status==='active',role:'employee'}});if(r.error||r.data?.error)dbError=r.error||new Error(r.data.error)}if(dbError)setError(dbError.message||'تعذر الحفظ');else{setShow(false);await load()}setSaving(false)}
 function openBranchAdd(){setEditingBranch(null);setBranchForm({...blankBranch});setError('');setShowBranch(true)}
 function openBranchEdit(r:any){setEditingBranch(r);setBranchForm({name:r.name||'',address:r.address||'',phone:r.phone||'',is_active:!!r.is_active});setError('');setShowBranch(true)}
 async function saveBranch(){if(!supabase)return;if(!branchForm.name.trim())return setError('اسم الفرع مطلوب');setSaving(true);setError('');const payload={name:branchForm.name.trim(),address:branchForm.address||null,phone:branchForm.phone||null,is_active:!!branchForm.is_active};const result=editingBranch?await supabase.from('hr_v2_branches').update(payload).eq('id',editingBranch.id):await supabase.from('hr_v2_branches').insert(payload);if(result.error)setError(result.error.message.includes('unique')?'اسم الفرع موجود بالفعل':result.error.message);else{setShowBranch(false);await load()}setSaving(false)}
 if(branchesOnly)return <><div className="page-head"><div><h2>الفروع</h2><p>إدارة الفروع وحالتها.</p></div><button className="primary" onClick={openBranchAdd}><Plus size={18}/>إضافة فرع</button></div><div className="panel table-wrap"><table><thead><tr><th>الاسم</th><th>العنوان</th><th>الهاتف</th><th>الحالة</th><th></th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.address||'—'}</td><td>{r.phone||'—'}</td><td>{r.is_active?'نشط':'متوقف'}</td><td><button className="icon-btn" onClick={()=>openBranchEdit(r)}><Edit3 size={17}/></button></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">لا توجد فروع بعد.</div>}</div>{showBranch&&<div className="modal-backdrop"><div className="modal"><h3>{editingBranch?'تعديل فرع':'إضافة فرع جديد'}</h3><div className="form-grid"><label>اسم الفرع *<input value={branchForm.name} onChange={e=>setBranchForm({...branchForm,name:e.target.value})}/></label><label>الهاتف<input value={branchForm.phone} onChange={e=>setBranchForm({...branchForm,phone:e.target.value})}/></label><label className="full">العنوان<input value={branchForm.address} onChange={e=>setBranchForm({...branchForm,address:e.target.value})}/></label><label>الحالة<select value={branchForm.is_active?'active':'inactive'} onChange={e=>setBranchForm({...branchForm,is_active:e.target.value==='active'})}><option value="active">نشط</option><option value="inactive">متوقف</option></select></label></div>{error&&<div className="error">{error}</div>}<div className="modal-actions"><button className="secondary" onClick={()=>setShowBranch(false)}>إلغاء</button><button className="primary" disabled={saving} onClick={saveBranch}>حفظ الفرع</button></div></div></div>}</>;
 return <><div className="page-head"><div><h2>الموظفون</h2><p>الملف الوظيفي والمرتب وساعات العمل وحساب الدخول.</p></div><button className="primary" onClick={openAdd}><Plus size={18}/>إضافة موظف</button></div><div className="toolbar"><div className="search"><Search size={18}/><input placeholder="بحث بالاسم أو الموبايل" value={q} onChange={e=>setQ(e.target.value)}/></div></div><div className="panel table-wrap"><table><thead><tr><th>رقم الموظف</th><th>الاسم</th><th>الوظيفة</th><th>الفرع</th><th>المرتب/الساعة</th><th>الحد اليومي</th><th>الدليفري</th><th>الحالة</th><th></th></tr></thead><tbody>{rows.filter(r=>(r.full_name_ar||'').includes(q)||(r.phone||'').includes(q)).map(r=><tr key={r.id}><td>{r.employee_no||'—'}</td><td>{r.full_name_ar}</td><td>{r.position||'—'}</td><td>{branches.find(b=>b.id===r.branch_id)?.name||'—'}</td><td>{Number(r.base_salary||0).toLocaleString('ar-EG')} ج.م</td><td>{Number(r.max_daily_paid_hours||r.daily_work_hours||8)} ساعة</td><td>{r.is_delivery?'نعم':'لا'}</td><td>{r.status==='active'?'نشط':r.status==='suspended'?'موقوف':'غير مفعل'}</td><td><button className="icon-btn" onClick={()=>openEdit(r)}><Edit3 size={17}/></button></td></tr>)}</tbody></table>{!rows.length&&<div className="empty">لا توجد بيانات بعد.</div>}</div>{show&&<div className="modal-backdrop"><div className="modal large"><h3>{editing?'تعديل موظف':'إضافة موظف جديد'}</h3><div className="form-grid"><label>رقم الموظف<input value={form.employee_no} onChange={e=>setForm({...form,employee_no:e.target.value})}/></label><label>الاسم بالعربي *<input value={form.full_name_ar} onChange={e=>setForm({...form,full_name_ar:e.target.value})}/></label><label>الاسم بالإنجليزي<input value={form.full_name_en} onChange={e=>setForm({...form,full_name_en:e.target.value})}/></label><label>الموبايل<input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value,login_phone:e.target.value})}/></label><label>الوظيفة<input value={form.position} onChange={e=>setForm({...form,position:e.target.value})}/></label><label>الفرع<select value={form.branch_id} onChange={e=>setForm({...form,branch_id:e.target.value})}><option value="">اختر الفرع</option>{branches.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label><label>المرتب الأساسي / سعر الساعة<input type="number" min="0" value={form.base_salary} onChange={e=>setForm({...form,base_salary:e.target.value})}/></label><label>نوع المرتب<select value={form.pay_type} disabled={form.is_delivery} onChange={e=>setForm({...form,pay_type:e.target.value})}><option value="monthly">شهري</option><option value="daily">يومي</option><option value="hourly">بالساعة</option></select></label><label>سعر ساعة الإضافي<input type="number" min="0" value={form.overtime_rate} onChange={e=>setForm({...form,overtime_rate:e.target.value})}/></label><label>ساعات العمل المحددة يوميًا<input type="number" min="1" max="24" value={form.daily_work_hours} onChange={e=>setForm({...form,daily_work_hours:e.target.value})}/></label><label>أقصى ساعات مدفوعة بدون موافقة<input type="number" min="1" max="24" value={form.max_daily_paid_hours} onChange={e=>setForm({...form,max_daily_paid_hours:e.target.value})}/></label><label>بداية العمل<input type="time" value={form.start} onChange={e=>setForm({...form,start:e.target.value})}/></label><label>نهاية العمل<input type="time" value={form.end} onChange={e=>setForm({...form,end:e.target.value})}/></label><label>تاريخ التعيين<input type="date" value={form.hire_date||''} onChange={e=>setForm({...form,hire_date:e.target.value})}/></label><label>الحالة<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="inactive">غير مفعل</option><option value="active">نشط</option><option value="suspended">موقوف</option></select></label></div><div className="login-section"><h4>حساب الدخول</h4><div className="form-grid"><label>موبايل الدخول<input value={form.login_phone} onChange={e=>setForm({...form,login_phone:e.target.value})}/></label><label>{editing?'كلمة مرور جديدة (اختياري)':'كلمة المرور *'}<input type="password" value={form.login_password} onChange={e=>setForm({...form,login_password:e.target.value})}/></label></div></div><div className="login-section"><h4>إعداد الدليفري</h4><label><input type="checkbox" checked={!!form.is_delivery} onChange={e=>setForm({...form,is_delivery:e.target.checked,pay_type:e.target.checked?'hourly':form.pay_type,payroll_cycle:e.target.checked?'weekly_delivery':'monthly'})}/> هذا الموظف دليفري — حسابه أسبوعي من السبت إلى الجمعة حسب ساعات العمل المسجلة</label>{form.is_delivery&&<div className="hint">سيتم عرض ساعات الدليفري للمحاسب، والمرتب الأسبوعي يحسب من الساعات المسجلة × سعر الساعة.</div>}</div>{editing&&<div className="hint">حساب الدخول: {editing.account_id?(editing.account_active?'نشط':'متوقف'):'غير موجود'}</div>}{error&&<div className="error">{error}</div>}<div className="modal-actions"><button className="secondary" onClick={()=>setShow(false)}>إلغاء</button><button className="primary" disabled={saving} onClick={saveEmployee}>{saving?'جاري الحفظ...':'حفظ الموظف'}</button></div></div></div>}</>;
}
