import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';export default function Attendance(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase?.from('hr_v2_attendance').select('*').order('work_date',{ascending:false}).limit(100).then(({data})=>setRows(data||[]))},[]);return <><div className="page-head"><h2>الحضور والانصراف</h2></div><div className="panel table-wrap"><table><thead><tr><th>الموظف</th><th>التاريخ</th><th>الحضور</th><th>الانصراف</th><th>الدقائق</th><th>الحالة</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.employee_id}</td><td>{r.work_date}</td><td>{r.check_in||'—'}</td><td>{r.check_out||'—'}</td><td>{r.work_minutes}</td><td>{r.status}</td></tr>)}</tbody></table></div></>}