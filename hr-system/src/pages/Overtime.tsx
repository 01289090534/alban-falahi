import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';export default function Overtime(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase?.from('hr_v2_overtime').select('*').order('work_date',{ascending:false}).limit(100).then(({data})=>setRows(data||[]))},[]);return <><div className="page-head"><h2>الإضافي</h2></div><div className="panel table-wrap"><table><thead><tr><th>الموظف</th><th>التاريخ</th><th>الساعات</th><th>السعر</th><th>المبلغ</th><th>الحالة</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.employee_id}</td><td>{r.work_date}</td><td>{r.hours}</td><td>{r.hourly_rate}</td><td>{r.amount}</td><td>{r.approval_status}</td></tr>)}</tbody></table></div></>}