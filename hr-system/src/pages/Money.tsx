import {useEffect,useState} from 'react';import {supabase} from '../lib/supabase';export default function Money(){const[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase?.from('hr_v2_money_transactions').select('*').order('transaction_date',{ascending:false}).limit(100).then(({data})=>setRows(data||[]))},[]);return <><div className="page-head"><h2>السلف والخصومات</h2></div><div className="panel table-wrap"><table><thead><tr><th>الموظف</th><th>التاريخ</th><th>النوع</th><th>المبلغ</th><th>السبب</th><th>الحالة</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.employee_id}</td><td>{r.transaction_date}</td><td>{r.type}</td><td>{r.amount}</td><td>{r.reason||'—'}</td><td>{r.approval_status}</td></tr>)}</tbody></table></div></>}