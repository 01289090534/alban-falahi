import {NavLink,useNavigate} from 'react-router-dom';import {Building2,Clock3,LogOut,ReceiptText,Settings,Timer,Users,WalletCards,LayoutDashboard,BarChart3} from 'lucide-react';import {supabase} from '../lib/supabase';import type {Profile} from '../types';
const menu=[['/','الرئيسية',LayoutDashboard],['/employees','الموظفون',Users],['/branches','الفروع',Building2],['/attendance','الحضور والانصراف',Clock3],['/overtime','الإضافي',Timer],['/money','السلف والخصومات',WalletCards],['/payroll','الرواتب',ReceiptText],['/reports','التقارير',BarChart3],['/settings','الإعدادات',Settings]] as const;
export default function Layout({profile,children}:{profile:Profile;children?:React.ReactNode}){const nav=useNavigate();async function logout(){await supabase?.auth.signOut();nav('/login')}if(profile.role==='employee')return <div className="content"><h2>بوابة الموظف</h2><p>حساب الموظف متاح من لوحة الموظف.</p><button className="logout" onClick={logout}><LogOut size={18}/>تسجيل الخروج</button></div>;return <div className="app-shell"><aside className="sidebar"><div className="brand"><div className="logo">ف</div><div><b>ألبان فلاحي</b><small>الموارد البشرية</small></div></div><nav>{menu.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>isActive?'active':''}><Icon size={19}/><span>{label}</span></NavLink>)}</nav><button className="logout" onClick={logout}><LogOut size={18}/>تسجيل الخروج</button></aside><main className="main"><header className="topbar"><div><strong>{profile.full_name}</strong><span className="role">{profile.role==='admin'?'مدير النظام':'محاسب'}</span></div><div className="top-title">نظام إدارة الموارد البشرية</div></header><section className="content">{children}</section></main></div>}