import {NavLink,Outlet,useNavigate} from 'react-router-dom';
import {Building2,Clock3,LogOut,ReceiptText,Settings,Timer,Users,WalletCards,LayoutDashboard,BarChart3,Menu,X,ShieldCheck} from 'lucide-react';
import {useState} from 'react';import {supabase} from '../lib/supabase';import type {Profile} from '../types';import logoData from '../assets/logoData';
const adminMenu=[['/','الرئيسية',LayoutDashboard],['/employees','الموظفون',Users],['/branches','الفروع',Building2],['/attendance','الحضور والانصراف',Clock3],['/overtime','الإضافي',Timer],['/money','السلف والخصومات',WalletCards],['/payroll','الرواتب',ReceiptText],['/reports','التقارير',BarChart3],['/users','المستخدمون والصلاحيات',ShieldCheck],['/settings','الإعدادات',Settings]] as const;
const accountantMenu=adminMenu.filter(([to])=>!['/employees','/branches','/users'].includes(to));
const employeeMenu=adminMenu.filter(([to])=>['/','/attendance','/overtime','/money','/payroll','/settings'].includes(to));
export default function Layout({profile}:{profile:Profile}){const nav=useNavigate();const[open,setOpen]=useState(false);async function logout(){await supabase?.auth.signOut();nav('/login')}const visible=profile.role==='admin'?adminMenu:profile.role==='accountant'?accountantMenu:employeeMenu;return <div className="app-shell"><button className="mobile-menu" aria-label="فتح القائمة" onClick={()=>setOpen(true)}><Menu size={25}/></button><aside className={`sidebar ${open?'open':''}`}><div className="brand"><img src={logoData} alt="ألبان فلاحي"/><div><b>ألبان فلاحي</b><small>الموارد البشرية</small></div><button className="mobile-close" aria-label="إغلاق القائمة" onClick={()=>setOpen(false)}><X size={22}/></button></div><nav>{visible.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/' } onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><Icon size={19}/><span>{label}</span></NavLink>)}</nav><button className="logout" onClick={logout}><LogOut size={18}/>تسجيل الخروج</button></aside><main className="main"><header className="topbar"><div className="top-user"><img src={logoData} alt=""/><div><strong>{profile.full_name}</strong><span className="role">{profile.role==='admin'?'مدير النظام':profile.role==='accountant'?'محاسب':'موظف'}</span></div></div><div className="top-title">ألبان فلاحي • نظام إدارة الموارد البشرية</div></header><section className="content"><Outlet/></section></main></div>}
