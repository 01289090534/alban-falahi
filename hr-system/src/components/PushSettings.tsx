import {useEffect,useState} from 'react';import {Smartphone,BellRing} from 'lucide-react';import {enablePushNotifications,isPushEnabled} from '../lib/push';
export default function PushSettings(){const[enabled,setEnabled]=useState(false);const[checked,setChecked]=useState(false);const[busy,setBusy]=useState(false);const[msg,setMsg]=useState('');const[err,setErr]=useState('');useEffect(()=>{isPushEnabled().then(setEnabled).catch(()=>{}).finally(()=>setChecked(true))},[]);async function setup(){setBusy(true);setErr('');setMsg('');try{await enablePushNotifications();setEnabled(true);setMsg('تم تفعيل إشعارات التقفيلات على هذا الموبايل ✅')}catch(e:any){setErr(e?.message||'تعذر تفعيل الإشعارات')}finally{setBusy(false)}}return <div className="panel" style={{marginBottom:16}}><div style={{display:'flex',alignItems:'center',gap:10,marginBottom:8}}><BellRing size={22}/><h3 style={{margin:0}}>إشعارات التقفيلات</h3></div><p className="hint">يتم إرسال تنبيه للمالك أو مدير النظام فور إضافة تقفيلة وردية جديدة.</p><div style={{display:'flex',alignItems:'center',justifyContent:'space-between',gap:12,flexWrap:'wrap',padding:'12px 0'}}><b>{checked?(enabled?'🟢 الإشعارات مفعلة على هذا الموبايل':'🔴 الإشعارات غير مفعلة'):'جاري التحقق...'}</b><button type="button" className="primary" disabled={busy||enabled} onClick={setup}><Smartphone size={17}/>{busy?'جاري التفعيل...':enabled?'الإشعارات مفعلة':'تفعيل الإشعارات'}</button></div>{msg&&<div className="success">{msg}</div>}{err&&<div className="error">{err}</div>}</div>}