self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?.text()||'وصل طلب جديد'}};const title=data.title||'🥛 طلب جديد - ألبان فلاحي';const options={body:data.body||'وصل طلب جديد للفرع',tag:data.tag||'alban-falahi-order',renotify:true,requireInteraction:true,vibrate:[300,100,300,100,500],data:{url:data.url||'/admin/'}};event.waitUntil(self.registration.showNotification(title,options))});self.addEventListener('notificationclick',event=>{event.notification.close();const url=event.notification.data?.url||'/admin/';event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if('focus' in c){c.focus();return c.navigate(url)}}return clients.openWindow(url)}))});